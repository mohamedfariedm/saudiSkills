export const tablesNames = {
  categories: "categories",
  branches: "branches",
  orders: "orders",
};

export const REGEX = {
  ENGLISH_LETTERS: /^[^\u0600-\u06FF]+$/,
  ARABIC_LETTERS: /^[^A-Za-z]+$/,
  NUMERIC: /^[0-9]+$/,
};
